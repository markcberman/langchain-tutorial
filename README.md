# LangChain + OpenAI (GPT-5 Mini) Learning Project

## Setup

```bash
conda env create -f environment.yaml
conda activate langchain
uv sync
jupyter lab

Notes

Uses OpenAI Responses API

LangChain v1 APIs only

Deterministic model: gpt-5-mini